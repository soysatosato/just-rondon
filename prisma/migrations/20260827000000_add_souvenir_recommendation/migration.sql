-- お土産の詳細ページに「どれを買うか」を足す。
--
-- 詳細ページは body(その品は何か) / givingNote(誰に渡すか) /
-- carryingNote(どう持ち帰るか)の3本で成り立っていたが、読者が店頭で
-- 最後に詰まるのはそのどれでもなく**銘柄**だった。棚に似た品が並んだ
-- ときに選べないと、記事を読んだ意味が半分になる。
--
-- 例: クロテッドクリームは Rodda's が最も知られているが要冷蔵で
-- 日本には持ち帰れない。持ち帰るなら Devon Cream Company の常温品を
-- 選ぶ必要がある——この分岐は「クロテッドクリームとは何か」を
-- どれだけ厚く書いても出てこない。
--
-- body に混ぜず独立させたのは、ここだけが「店頭で指させる粒度」を
-- 扱うため。商品は入れ替わるので、更新が要るのはこの列に集まる。

ALTER TABLE "Souvenir" ADD COLUMN IF NOT EXISTS "recommendation" TEXT;
